# สรุปวิดีโอจากโฟลเดอร์ “พี่ฝึกงาน”

ตรวจทั้งหมด 11 คลิป ความยาวรวมประมาณ 3 นาที เนื้อหาส่วนใหญ่เป็นอินโฟกราฟิก/คลิปสั้นเกี่ยวกับ AI Agent, Claude และการพัฒนาซอฟต์แวร์

## สรุปภาพรวมแบบสั้น

แนวคิดหลักของชุดวิดีโอคือ **การสร้างระบบ AI Agent ให้ทำงานเป็นระบบและเลือกเครื่องมือให้เหมาะกับงาน** ตั้งแต่การเชื่อมต่อเครื่องมือด้วย MCP, เพิ่มความสามารถด้วย Skill หรือ Plugin, แบ่งงานให้ Subagent/Agent Team, ออกแบบ workflow ที่ตรวจสอบและวนแก้ไขได้ ตลอดจนเลือกฐานข้อมูลและเทคโนโลยี Full-stack ให้เหมาะกับระบบ

## แต่ละคลิปเกี่ยวกับอะไร

### 1) MCP 101 — โปรโตคอลกลางสำหรับ AI Agent

ไฟล์: `28241796-2398-48c2-bce6-a5d2d8f14efe.mp4` (ประมาณ 8 วินาที)

- MCP หรือ Model Context Protocol เป็นชั้นกลางที่ทำให้โมเดลเชื่อมต่อเครื่องมือและแหล่งข้อมูลผ่านรูปแบบเดียวกัน
- ต่างจาก API แบบเดิมที่ออกแบบให้โปรแกรมเรียกใช้งานด้วยคำสั่งตายตัว ส่วน MCP ช่วยให้โมเดลมองเห็นคำอธิบายของเครื่องมือ แล้วเลือกใช้ตามบริบทของงานได้
- ลำดับการทำงานโดยย่อ: client เชื่อมต่อ → server ประกาศ tools/resources/prompts → โมเดลวางแผน → เรียกเครื่องมือเพื่อทำงาน
- จำง่าย ๆ: MCP ทำหน้าที่คล้าย “หัวแปลงมาตรฐาน” ระหว่าง AI กับเครื่องมือหรือข้อมูลหลายชนิด

### 2) Skill กับ Plugin ต่างกันอย่างไร

ไฟล์: `2cd36c9c-3168-48fa-a8e8-3ccd57b4fb13.mp4` (ประมาณ 16 วินาที)

- **Skill** คือความสามารถหรือวิธีทำงานที่บรรจุอยู่ในไฟล์เดียว เมื่อ Agent อ่านก็ทำงานตามวิธีนั้นได้ เหมาะกับงานเฉพาะและเริ่มทำได้เร็ว
- **Plugin** คือแพ็กเกจใหญ่ที่รวมได้หลายส่วน เช่น skills, hooks, agents และ MCP ติดตั้งครั้งเดียวแล้วแจกจ่ายให้ทั้งทีมใช้ได้
- แนวคิดของคลิป: เริ่มจาก Skill ให้ใช้งานได้จริงก่อน เมื่อระบบโตหรือหลายคนต้องใช้จึงค่อยแพ็กเป็น Plugin

### 3) การออกแบบ Control Plane ของ Agent

ไฟล์: `57d76666-d503-452e-a634-0be5a6820d56.mp4` (ประมาณ 4 วินาที)

- `CLAUDE.md` เก็บคำสั่งระดับโปรเจกต์ที่ควรรู้ตลอด เช่น architecture, คำสั่ง build/test และข้อตกลงของทีม โดยควรเขียนให้สั้น
- `rules/` เก็บกฎที่โหลดตามขอบเขต เช่น frontend pattern, API contract หรือข้อกำหนดฐานข้อมูล
- `skills/` เก็บ playbook ที่ใช้ซ้ำ เช่น deploy, launch copy, SEO audit และ release notes
- `agents/` แยกผู้เชี่ยวชาญเฉพาะด้าน เช่น code reviewer, security auditor หรือ researcher เหมาะกับงานที่ไม่ควรทำให้ context หลักรก
- `hooks` ใช้ควบคุมงานแบบแน่นอน เช่น format หลังแก้ไฟล์, บล็อก secrets, รัน test และบันทึก action
- ใจความสำคัญคือวางคำสั่งให้ถูกชั้น เพื่อไม่เปลือง context และควบคุมพฤติกรรม Agent ได้ดี

### 4) Agent Office — ภาพจำลองทีม AI Agent ในออฟฟิศ 3 มิติ

ไฟล์: `698174b4-dfd0-4645-b8a2-2a89517e8802.mp4` (ประมาณ 1 นาที 46 วินาที)

- เป็นเดโมหน้าจอที่แทน Agent หลายตัวเป็นตัวละครในออฟฟิศ ทำให้เห็นว่าแต่ละตัวกำลังทำงานอะไรและอยู่ในสถานะใด
- มีบทบาทตัวอย่าง เช่น Radar, Analyst, Fact Check และ Publisher สื่อถึงกระบวนการค้นหา → วิเคราะห์ → ตรวจสอบ → เผยแพร่
- จุดเด่นของแนวคิดนี้คือทำให้ multi-agent workflow ที่ปกติมองไม่เห็น กลายเป็นภาพที่ติดตาม ตรวจสอบ และเข้าใจง่ายขึ้น
- ช่วงท้ายมีหน้ามุมมองแยกตาม Agent/บทบาทและสถานะงาน
- เสียงประกอบเป็น spoken word ภาษาอังกฤษแนวพัฒนาตัวเอง: การรักษาสัญญากับตัวเอง เลิกหาข้ออ้าง เลือกการเติบโต สร้างวินัย และสร้าง self-respect จากการลงมือทำเมื่อไม่มีใครมองอยู่

### 5) Database Selection Cheatsheet

ไฟล์: `75a3b7dd-a938-4806-9135-a4e95bdd39f8.mp4` (ประมาณ 9 วินาที)

- แผนผังช่วยเลือกฐานข้อมูลตามลักษณะงาน ไม่ได้ชี้ว่าฐานข้อมูลตัวใดดีที่สุดทุกกรณี
- Production/OLTP: relational เช่น PostgreSQL, MySQL, SQL Server หรือบริการ SQL ที่ scale ได้บน cloud
- NoSQL ตาม use case: document, key-value, wide-column, graph และ search
- Analytics/OLAP: data warehouse และ lakehouse/big data เช่น Snowflake, BigQuery, Redshift, Databricks และ S3/Delta Lake
- ยังมี specialized database เช่น time-series รวมถึงกลุ่ม hybrid/emerging เช่น vector database และ real-time analytics
- หลักคิดสำหรับสัมภาษณ์: เลือกจาก workload, รูปแบบข้อมูล/การ query, consistency, scale, latency และความถนัดของทีม

### 6) เลือกโหมดใช้งาน Claude ให้เหมาะกับงาน

ไฟล์: `75e44145-3648-421a-bd20-e0c624a4f73b.mp4` (ประมาณ 9 วินาที)

- **Claude Chat**: เหมาะกับถาม-ตอบเร็ว ระดมไอเดีย แชร์บริบท และปรับคำตอบเป็นรอบ ๆ
- **Claude Cowork**: เหมาะกับงานแบบโปรเจกต์และการร่วมมือ มีบริบทระดับโปรเจกต์และความรู้ต่อเนื่อง
- **Claude CLI**: เหมาะกับ power user, automation, terminal workflow และการ build/test ให้เร็วขึ้น
- แนวปฏิบัติที่ดี: บอกเป้าหมายและบริบทให้ชัด ใช้ตัวอย่าง แยกโปรเจกต์สำหรับงานใหญ่ ทบทวนบริบท และค่อย ๆ iterate

### 7) Full-stack Web Development Roadmap

ไฟล์: `c00cdf82-016d-4174-b855-83b4405f8a9b.mp4` (ประมาณ 10 วินาที)

- Frontend: HTML, CSS, JavaScript, TypeScript; React/Redux/jQuery; Next.js, Angular และ Vue
- Backend: API แบบ REST, GraphQL, gRPC; ฐานข้อมูล PostgreSQL, MySQL, MongoDB; ภาษา/Runtime เช่น Java, Python, Node.js, Go และ PHP
- Hosting/CDN: ตัวอย่างเช่น Vercel, AWS และ Cloudflare
- ใจความคือ Full-stack ไม่ใช่จำทุกเทคโนโลยี แต่เข้าใจว่าระบบแบ่งเป็นส่วนใดและเลือก stack ให้เข้ากับโจทย์อย่างไร

### 8) หนึ่ง Claude กับ 3 วิธีขยายงาน

ไฟล์: `cc615e06-5068-4f11-954e-688e4bed6d6f.mp4` (ประมาณ 13 วินาที)

- **Agent เดี่ยว**: context เดียว วน plan → inspect → act เหมาะกับงานตรงไปตรงมา
- **Agent Team**: มี lead และ peer หลายบทบาท เช่น frontend, backend และ tests แบ่งงานร่วมกัน แต่ละตัวมี context ของตนเอง
- **Dynamic Workflow**: ขั้นตอนถูกกำหนดในโค้ด เช่น discover → fan out → check → fix → verify ถ้า test ไม่ผ่านสามารถวนกลับไปแก้ได้
- วิธีเลือก: งานเล็กใช้ Agent เดี่ยว, งานแยกขนานและต้องคุยกันใช้ Agent Team, งานที่ต้องทำซ้ำและต้องการ flow แน่นอนใช้ Dynamic Workflow

### 9) ภาพรวม AI Engineering

ไฟล์: `d2130c69-4c88-40d3-aceb-a65249256df8.mp4` (ประมาณ 8 วินาที)

- ฝั่ง **Build**: prompts, skills และ plugins/MCP เพื่อกำหนดพฤติกรรม เพิ่มความสามารถ และเชื่อมต่อไฟล์ ฐานข้อมูล API หรือบริการ
- ฝั่ง **Orchestrate**: execution patterns, agent teams, guardrails และ memory เช่น loop/workflow/graph, planner-worker-reviewer, rules/policies, RAG และ state management
- เป้าหมายสุดท้ายต้องวัดที่ outcome ได้แก่ accuracy, reliability, speed, cost efficiency และผลกระทบต่อผู้ใช้

### 10) Graph Engineering สำหรับ workflow ที่ปลอดภัย

ไฟล์: `f1e4d1ac-8c47-4278-bb30-81b734c3fee6.mp4` (ประมาณ 11 วินาที)

- เริ่มจาก mission เช่น “ส่ง feature ที่ปลอดภัย” แล้วให้ router เลือกเส้นทาง
- Researcher, Architect และ Builder ทำงานเฉพาะด้าน พร้อมเขียน facts/decisions/artifacts ลง shared state
- Integrator รวมงาน, Reviewer ตรวจ test/quality/safety, Human checkpoint อนุมัติงานเสี่ยงสูง แล้วจึง ship
- หากตรวจไม่ผ่าน ระบบวนกลับไป Builder เพื่อแก้ ไม่ได้เดินหน้าอย่างเดียว
- องค์ประกอบสำคัญของกราฟคือ nodes, edges, state และ conditions

### 11) Skill vs Subagent vs Agent Team

ไฟล์: `fc3a1cc2-e425-434b-8fc2-f3240055dd37.mp4` (ประมาณ 11 วินาที)

- **Skill — in-context**: Agent เดิมใช้วิธีทำงานที่นำกลับมาใช้ซ้ำได้ overhead ต่ำ เหมาะเมื่อแค่ต้องเพิ่ม capability
- **Subagent — isolated**: แยก context ใหม่ ทำงานขอบเขตชัด แล้วส่งผลลัพธ์กลับ เหมาะกับงานขนานหรืองานที่ต้องการความสะอาดของ context
- **Agent Team — peers**: หลาย Agent มี context แยกกันและประสานกันโดยตรง เหมาะกับงานที่ต้องร่วมมือหลายบทบาท
- กฎจำง่าย: เลือกโครงสร้างที่เบาที่สุดซึ่งทำงานสำเร็จได้ — capability → isolation → coordination; จำนวน Agent มากขึ้นไม่ได้แปลว่า automation ดีขึ้นเสมอ

## แก่นที่ควรเข้าใจก่อนไปสัมภาษณ์

1. **AI Agent ไม่ใช่แค่ chatbot** แต่เป็นโมเดลที่มีเป้าหมาย วางแผน ใช้เครื่องมือ อ่านข้อมูล และตรวจผลลัพธ์ได้
2. **MCP คือมาตรฐานการเชื่อมต่อ** ส่วน Skill คือวิธีทำงานเฉพาะ และ Plugin คือแพ็กเกจที่รวมความสามารถหลายอย่าง
3. **การแบ่งหลาย Agent มีต้นทุน** ทั้ง context, coordination และความซับซ้อน จึงควรใช้เท่าที่จำเป็น
4. **Workflow ที่ดีต้องตรวจสอบได้** มี state, เงื่อนไข, test/reviewer, retry และ human checkpoint สำหรับจุดเสี่ยง
5. **เลือกเทคโนโลยีจากโจทย์** ไม่ใช่เลือกเพราะกำลังดัง เช่น ฐานข้อมูลต้องดู workload และระบบ AI ต้องดู accuracy, reliability, speed, cost และ user impact

## ตัวอย่างคำตอบถ้าถูกถามว่า “ได้อะไรจากวิดีโอชุดนี้”

> ผมเข้าใจภาพรวมของการสร้าง AI Agent มากขึ้นครับ ตั้งแต่การเชื่อมเครื่องมือผ่าน MCP การเพิ่มวิธีทำงานด้วย Skill หรือแพ็กเป็น Plugin ไปจนถึงการเลือกว่าเมื่อไรควรใช้ Agent เดี่ยว, Subagent หรือ Agent Team สิ่งที่ผมคิดว่าสำคัญที่สุดคือไม่ควรเพิ่ม Agent หรือเครื่องมือโดยไม่จำเป็น แต่ควรออกแบบ workflow ให้ตรวจสอบได้ มี shared state, test/reviewer, retry และ human checkpoint แล้ววัดผลที่ความถูกต้อง ความน่าเชื่อถือ ความเร็ว ต้นทุน และผลต่อผู้ใช้ครับ

## คำถามที่ใช้ถามพี่ตอนสัมภาษณ์ได้

- ในงานจริง พี่ใช้เกณฑ์อะไรตัดสินว่าโจทย์ไหนควรใช้ Agent เดี่ยว, Subagent หรือ Agent Team?
- Agent Office ในคลิปเป็นโปรเจกต์ที่พี่ทำเองหรือเป็นเครื่องมือที่พี่กำลังศึกษา และแก้ปัญหาอะไรให้ทีม?
- ระบบ multi-agent เก็บ shared state และป้องกันข้อมูลขัดแย้งกันอย่างไร?
- ใน workflow จริงมีจุดใดที่ให้ AI ตัดสินใจเอง และจุดใดต้องมี human checkpoint?
- ทีมวัดคุณภาพ Agent จาก metric อะไรบ้าง เช่น accuracy, latency, cost หรือ task completion rate?
- MCP server ที่ใช้จริงเชื่อมกับเครื่องมือหรือฐานข้อมูลอะไร และจัดการสิทธิ์/ความปลอดภัยอย่างไร?
- หาก Agent ทำงานผิดหรือ test ไม่ผ่าน ระบบ retry, rollback หรือส่งต่อให้คนอย่างไร?
- สำหรับนักศึกษาฝึกงาน พี่คาดหวังพื้นฐานด้าน Web, Database และ AI Agent ในระดับไหน?

## คำศัพท์จำง่าย

- **Prompt**: คำสั่ง/บริบทที่บอก AI ว่าต้องทำอะไร
- **Tool**: ความสามารถที่ Agent เรียกใช้ เช่น ค้นไฟล์ เรียก API หรือรัน test
- **MCP**: มาตรฐานที่ทำให้ AI เชื่อมต่อ tools/resources/prompts ได้เป็นรูปแบบเดียว
- **Skill**: วิธีทำงานหรือ playbook ที่ Agent นำไปใช้ซ้ำ
- **Plugin**: แพ็กเกจรวมความสามารถหลายชนิดเพื่อแจกจ่าย/ติดตั้ง
- **Subagent**: Agent ย่อยที่มี context แยกและรับงานขอบเขตชัด
- **Agent Team**: Agent หลายตัวที่ประสานงานกันในฐานะเพื่อนร่วมทีม
- **Shared State**: พื้นที่กลางเก็บข้อเท็จจริง การตัดสินใจ และผลลัพธ์
- **Guardrail**: กฎหรือข้อจำกัดเพื่อให้ระบบปลอดภัยและคาดเดาได้
- **Human Checkpoint**: จุดที่ต้องให้คนตรวจหรืออนุมัติก่อนเดินหน้าต่อ
